import React from 'react';
import background from '../../react-redux.jpg'

const HomePage = () => {
    return (
        <div className='react-home' style={{backgroundImage: `url(${background})`}}>

            
        </div>
    );
};

export default HomePage;